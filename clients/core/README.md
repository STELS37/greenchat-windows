# clients/core — client SDK

Zero runtime dependencies. Modules (added across Phase F): `api`, `ws`, `sync`, `store`, `outbox`,
`files`, `push-bridge`. Tested with `node --test` against a live local server (Node 22 native
`fetch`/`WebSocket`). See `docs/CLIENTS.md` §4.
